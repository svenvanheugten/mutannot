# Mutannot.Annotations

Provides the `ShouldCatch` attribute for [mutannot](https://github.com/svenvanheugten/mutannot), a tool that lets you write the mutations that should cause a test to fail directly into your test code.

